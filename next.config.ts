// next.config.js
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["img.icons8.com", "i.postimg.cc", "via.placeholder.com"], // Agrega el dominio aquí
  },
};

export default nextConfig;
